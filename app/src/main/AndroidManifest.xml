<?xml version="1.0" encoding="utf-8"?><!-- ~ Copyright (c) 2017. kazi srabon. Contact : kaziiit@gmail.com -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ks.ecmanager.ecouriermanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.initLayer.ActivityLogin"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustPan|adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.firstLayer.ActivityMain"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustPan|adjustNothing" />
        <activity android:name=".activities.firstLayer.ActivityProfile" />
        <activity android:name=".activities.secondLayer.ActivityConsignment" />
        <activity android:name=".activities.secondLayer.ActivityConsignmentDetails" />
        <activity android:name=".KotlinActivity" />
        <activity android:name=".activities.firstLayer.Main2Activity" />
        <activity android:name=".activities.secondLayer.ActivityDelivery"></activity>
    </application>

</manifest>